import { UserCardDirective } from './user-card.directive';

describe('UserCardDirective', () => {
  it('should create an instance', () => {
    const directive = new UserCardDirective();
    expect(directive).toBeTruthy();
  });
});
